name: CI

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # Daily at 03:00 UTC
    - cron: "0 3 * * *"

env:
  CARGO_TERM_COLOR: always

jobs:
  checks:
    name: Format, lint, and test
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            .
            fuzz

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Lint
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Run tests
        run: cargo test --all-features

      - name: Check fuzz targets compile
        run: cargo check --manifest-path fuzz/Cargo.toml

      - name: Build release binary
        run: cargo build --release --locked

  fuzz-smoke:
    name: Fuzz smoke test
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install Rust nightly toolchain
        uses: dtolnay/rust-toolchain@nightly

      - name: Cache build artifacts
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-fuzz
        run: cargo install cargo-fuzz --locked

      - name: Fuzz lexer (10 minutes)
        run: cargo +nightly fuzz run lexer -- -max_total_time=600
        working-directory: fuzz

      - name: Fuzz parser (10 minutes)
        run: cargo +nightly fuzz run parser -- -max_total_time=600
        working-directory: fuzz

      - name: Fuzz pipeline (10 minutes)
        run: cargo +nightly fuzz run pipeline -- -max_total_time=600
        working-directory: fuzz
